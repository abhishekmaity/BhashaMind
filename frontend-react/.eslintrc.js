module.exports = {
  extends: [
    'react-app', // Uses the recommended rules from eslint-config-react-app
    'react-app/jest' // Includes Jest-specific linting rules
  ],
  // You can override rules here if needed
  // rules: {
  //   'some-rule': 'off'
  // }
};
